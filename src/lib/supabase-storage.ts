// Re-export functions from supabase-config for backward compatibility
export { 
  SUPABASE_CONFIG,
  buildSupabaseUrl,
  buildProductImageUrl,
  buildAssetUrl,
  buildContentUrl
} from './supabase-config';
